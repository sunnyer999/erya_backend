# Spring properties
spring:
  application:
    name: crawler-service  # Identify this application

# Map the error path to error template
error:
  path=/error

server:
  port: 8081   # HTTP (Jetty) port

# Log config
logging:
  level:
    root: INFO
  file: ./crawler-service.log